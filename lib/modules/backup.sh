#!/usr/bin/env bash
# Module: backup — Backup et restauration configs/BDD, rotation, copie distante, cron
# Dépendances: logging.sh, errors.sh, ssh.sh, utils.sh

backup_run() {
  log_info "Mode verbose actif"
  log_action "Commande backup non encore implémentée (Epic 5)"
  return 0
}

backup_restore() {
  log_info "Mode verbose actif"
  log_action "Commande restore non encore implémentée (Epic 5)"
  return 0
}
