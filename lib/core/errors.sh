#!/usr/bin/env bash
# Module: errors — Gestion d'erreurs, codes retour
# Dépendances: logging.sh

# Codes retour: 0=ok, 1=général, 2=SSH, 3=service non démarré
# Fonctions: die(), run_step(), trap unexpected_error
