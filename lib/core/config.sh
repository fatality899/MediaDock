#!/usr/bin/env bash
# Module: config — Chargement .env, mode interactif
# Dépendances: logging.sh, errors.sh

# Fonctions: load_config(), validate_config(), interactive_setup()
# Validation: require_var(), validate_choice(), validate_ip()
